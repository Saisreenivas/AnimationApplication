<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.saisreenivas.android.animationapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ValueAnimation"
            android:label="@string/title_activity_value_animation" />
        <activity
            android:name=".ValueAnimationFAB"
            android:label="@string/title_activity_value_animation" />
        <activity android:name=".ValueAnimationBorder" />
        <activity android:name=".ValAnimeXML" />
        <activity android:name=".ObjAnime" />
        <activity android:name=".ObjAnimeXML" />
        <activity android:name=".AnimeSet" />
        <activity android:name=".MultiAnimeOneView"></activity>
    </application>

</manifest>